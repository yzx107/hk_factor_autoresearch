[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hk-factor-autoresearch"
version = "0.1.0"
description = "Phase A semi-auto skeleton for controlled Hong Kong factor research"
readme = "README.md"
requires-python = ">=3.11"
